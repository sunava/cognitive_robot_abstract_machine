# The server and the live bridge avoid the heavy CRAM stack on purpose: the
# viewer must be servable on machines without it. krrood (EQL), coraplex and
# giskardpy are imported lazily and only needed for the knowledge panel, the
# onboarder and the live bridge hooks — inside this workspace they are present.
typing_extensions
