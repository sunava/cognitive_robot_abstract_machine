# scene bundles are generated by cram-viz-onboard (tens of MB each); ready-made
# recordings come from the *optional* cram-scenes submodule at ./scenes — see
# src/cram_viz/paths.py for the search order
